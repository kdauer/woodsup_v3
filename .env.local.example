# Sanity CMS Configuration
# Get these values from https://sanity.io/manage after creating your project

# Required for Sanity Studio and client
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id-here
NEXT_PUBLIC_SANITY_DATASET=production

# Required for migration scripts (write access)
SANITY_API_TOKEN=your-write-token-here

# Required for webhooks (instant content updates)
# Generate a random secret string (e.g., using: openssl rand -base64 32)
SANITY_WEBHOOK_SECRET=your-webhook-secret-here
